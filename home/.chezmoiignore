{{/*
  Exclude files that should not be materialized on the current platform.
*/ -}}

{{ if eq .chezmoi.os "darwin" -}}
dot_tmux.conf
{{- end }}

{{ if not .setupGit -}}
.gitconfig
.config/git/**
.ssh/config
{{- end }}
