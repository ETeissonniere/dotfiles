#! /bin/sh

if [ -n "$1" ]; then
    rsync -a --progress --delete $1/.ssh/* ~/.ssh/
    rsync -a --progress --delete $1/.gnupg/* ~/.gnupg/
    rsync -a --progress --delete $1/.password-store/* ~/.password-store/
else
    echo "Usage: restore <destination>"
fi
